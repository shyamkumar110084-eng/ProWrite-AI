import { ContentType, Tone, ContentLength } from './types';

export const CONTENT_TYPE_OPTIONS = Object.values(ContentType);
export const TONE_OPTIONS = Object.values(Tone);
export const CONTENT_LENGTH_OPTIONS = Object.values(ContentLength);
